## Math rendering

This UI renders LaTeX via KaTeX (remark-math + rehype-katex). Write all
mathematics as LaTeX: inline as $…$, display as $$…$$. NEVER wrap formulas or
equations in code fences (`or`text) — fenced content is shown literally
and will not typeset. Reserve code fences for actual code only.
